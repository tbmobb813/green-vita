[package]
name = "green-vita"
version = "0.1.8"
edition = "2024"

[dependencies]
anyhow = "1"
bytes = "1"
crossbeam-channel = "0.5"
egui = "0.31"
fluent-bundle = "0.15"
h264-reader = "0.8"
image = { version = "0.25", default-features = false, features = ["jpeg", "png"] }
qrcode = { version = "0.14", default-features = false }
reqwest = { version = "0.12", features = ["json", "rustls-tls"], default-features = false }
ring = "0.17"
rtc = "0.20.0-rc.2"
rtc-media = "0.20.0-rc.2"
rtcp = { version = "0.20.0-rc.2", package = "rtc-rtcp" }
sdl2 = { version = "0.38", features = ["use-pkgconfig", "unsafe_textures"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["macros", "net", "rt", "sync", "time"] }
unic-langid = "0.9"
uuid = { version = "1", features = ["v4", "serde"] }
vita-newlib-shims = "0.3"
vitasdk-sys = { version = "0.3", features = ["SceAppUtil_stub", "SceKernelDmacMgr_stub", "SceKernelThreadMgr_stub", "SceLibKernel_stub", "SceVideodec_stub", "SceSysmem_stub", "SceSysmodule_stub"] }

# Pinned to commit SHAs rather than branches: both are mutable refs on forks we don't
# control, so a branch pin would let an upstream force-push or new commit silently
# change what gets built.
[patch.crates-io]
ring = { git = "https://github.com/vita-rust/ring.git", rev = "502a916e86dce91ffa9ad96ef62a6e9fc6f96755" }
rtc-shared = { git = "https://github.com/Day-OS/rtc.git", rev = "8a407b3d99757016792c5979fcda4a90ac1485f5" }

[package.metadata.vita]
title_id = "GREENVITA"
title_name = "GreenVita"
assets = "static"

# Size/speed tuning for a 512 MB device with no room to spare. `panic = "abort"` is
# deliberately not set here: the video/catalog/RTC worker threads use `catch_unwind`
# to survive a single decode/parse panic without taking down the process, which
# requires the default unwind panic strategy. `strip = "symbols"` is also deliberately
# not set: cargo-vita's `vita-elf-create` step needs the ELF symbol table to build the
# velf, so stripping it here breaks the final link (`vita-gcc` rejects `-Wl,--strip-all`).
# Use `strip_symbols = true` under `[package.metadata.vita.release]` instead if
# post-link stripping of the velf is wanted.
[profile.release]
lto = "thin"
codegen-units = 1
strip = "debuginfo"
